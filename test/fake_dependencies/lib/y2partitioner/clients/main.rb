module Y2Partitioner
  module Clients
    # Just fake client from partitioner. It is entry point to partitioner and currently only API
    # that is used from partitioner in storage.
    class Main
      def self.run
      end
    end
  end
end
